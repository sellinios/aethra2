apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-frontend
  namespace: kairos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-frontend
  template:
    metadata:
      labels:
        app: react-frontend
    spec:
      containers:
      - name: react-frontend
        image: sellinios/react-frontend:latest
        ports:
        - containerPort: 80
